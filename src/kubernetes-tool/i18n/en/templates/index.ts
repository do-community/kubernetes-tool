import app from "./app"
import splashScreen from "./splash_screen"
import shared from "./shared"
import footer from "./footer"

export default { app, splashScreen, shared, footer } as {[key: string]: {[key: string]: string}}
