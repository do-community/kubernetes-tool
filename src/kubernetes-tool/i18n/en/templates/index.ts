import app from "./app"
import splashScreen from "./splash_screen"
import shared from "./shared"
import splitView from "./split_view"

export default { app, splashScreen, shared, splitView } as {[key: string]: {[key: string]: string}}
